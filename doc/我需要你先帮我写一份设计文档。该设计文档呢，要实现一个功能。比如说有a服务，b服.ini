我需要你先帮我写一份设计文档。该设计文档呢，要实现一个功能。比如说有a服务，b服务，后面有多个服务都是springboot jar 服务。  
我需要实现每个服务都要向其中一个master节点进行数据上报，那么中间可能上报的途径是通过radius集群。通过radius这个桥梁，
那我需要对每个服务的这个健康状态和运行状态都会啊有一个展现 ， 那这基本上就是一个所有集群服务的运行的一个监控，
那在master几点会有一个监控面板可以直接看到我现在所有的节点的运行状态。这里的运行状态就包含了很多方面，
比如说我当前的服务的啊，这个这个啊。内存使用率啊，内存使用情况啊，当前服务的这个啊，访问情况啊等等等等你能想到的一些监控。
那其次除了监控本身之外还需要一个功能，是说我在master节点呃有两项能力，那第1项能力呢，上传文件的能力，那第2项能力，自动进行包的下载，并且打包好，然后将包进行分发的能力。

关于文件同步方案，现在想到两种，第一种是通过共享目录来进行分发， 最简单
第二种是通过，redis ， 其他服务举手说我要升级， master节点知道大家要升级， 那我就推送过去。
如果你有更好的方案可以提出来，其中最重要的是能够分发jar包，关闭服务，启动服务，并且检测是否启动成功，如果失败了需要回滚
我需要一个专业的设计方案

一句话说明我要做功能，就是要实现监控集群种的jar服务是否正常，并且能够打包jar，分发到对应jar服务的节点，并且能够关闭服务，
启动服务，并且检测是否启动成功，如果失败了需要回滚，对分发能力，这个是核心功能，请不要忘记。比如a服务b服务都要升级， 但是jar包是不一样的，所以需要有根据jar进行分发的能力。
在这里agent是指每个jar服务中的一个程序，负责和master节点进行通信，并且负责执行master节点的指令。



我的环境
java8 必须
springboot2.7.18 必须
maven3.6.3 必须
redis 4.x 必须
其他都没有，不要给我加多余的

规则
我所有的代码逻辑都在controller种编写，不要在service种编写
用最简洁的代码实现逻辑。
所有的配置都放在application.yml中
所有的日志都放在logs目录下
